// src/components/Header.tsx
import React from "react";
import { Link } from "react-router-dom";
import { motion } from "framer-motion";

const Header: React.FC = () => {
  return (
    <header className="w-full py-4 px-6 flex items-center justify-between bg-gradient-to-r from-[#0D3B66] via-[#2A9D8F] to-[#F4A261] shadow-lg">
      <Link to="/" className="flex items-center gap-3">
        <motion.img
          src="/logo.svg"
          alt="AI Smart Survey Tool logo"
          className="w-12 h-12 drop-shadow-lg"
          initial={{ scale: 0.8, opacity: 0 }}
          animate={{ scale: 1, opacity: 1 }}
          transition={{ duration: 0.6, ease: "easeOut" }}
        />
        <motion.div
          className="flex flex-col"
          initial={{ y: -10, opacity: 0 }}
          animate={{ y: 0, opacity: 1 }}
          transition={{ duration: 0.6, delay: 0.2 }}
        >
          <div className="text-xl font-bold text-white drop-shadow-md">AI Smart Survey</div>
          <div className="text-sm text-white/90 italic">From Questions to Clarity</div>
        </motion.div>
      </Link>
      <nav>
        <motion.div whileHover={{ scale: 1.05 }}>
          <Link
            to="/survey"
            className="px-4 py-2 bg-white text-[#0D3B66] rounded-full font-medium shadow-md hover:shadow-lg transition"
          >
            Start Survey
          </Link>
        </motion.div>
      </nav>
    </header>
  );
};

export default Header;
